import styled from "styled-components";

export const StyledContainer = styled.div`
  width: 1024px;
  margin: 0 auto;

  .bold {
    font-weight: bold;
  }
  .add-row {
    cursor: pointer;
    svg {
      margin-left: 0.25rem;
      font-size: 1.25rem;
      vertical-align: middle;
    }
  }
`;
