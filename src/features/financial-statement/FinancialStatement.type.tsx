export type UserTypes = "guest" | "IT" | "Analyst";

export const UserRoles = {
  guest: "guest",
  IT: "IT",
  Analyst: "Analyst",
};

export type NewRowsCountType = {
  REVENUE: number;
  EXPENSE: number;
};
